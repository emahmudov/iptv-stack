[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "iptv-stack"
version = "0.1.0"
description = "Automated IPTV playlist builder and portal generator"
readme = "README.md"
requires-python = ">=3.9"
